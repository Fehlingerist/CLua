Problems to solve:
    -Define all the data types that I will have to return (
        NodeHandle {
            CompilerNode {TokenSpanNode,ErrorNode,NodeChain},
            UserDefinedNode {...}
        },
        Symbol //{different sets of symbols exist per language}
    )
    -Conversion of patterns to specific data types
    -InvertedPattern creating their own form of NodeChain type.